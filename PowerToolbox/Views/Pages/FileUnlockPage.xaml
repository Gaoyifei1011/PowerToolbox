<Page
    x:Class="PowerToolbox.Views.Pages.FileUnlockPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:class="using:PowerToolbox.Extensions.DataType.Class"
    xmlns:converter="using:PowerToolbox.Helpers.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enum="using:PowerToolbox.Extensions.DataType.Enums"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:PowerToolbox.Models"
    xmlns:rootservice="using:PowerToolbox.Services.Root"
    xmlns:sys="using:System"
    AllowDrop="True"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <CornerRadius x:Key="ListViewItemCornerRadius">0</CornerRadius>

            <SolidColorBrush x:Key="HyperlinkButtonBackgroundPointerOver" Color="Transparent" />
            <SolidColorBrush x:Key="HyperlinkButtonBackgroundPressed" Color="Transparent" />

            <class:ExecuteCommand x:Name="DeleteCommand" ExecuteRequested="{x:Bind OnDeleteExecuteRequested}" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="30,15,30,0"
            FontSize="22"
            Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileUnlock')}" />

        <TextBlock
            Grid.Row="1"
            Margin="32,3,32,3"
            FontSize="14"
            Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileUnlockDescription')}" />

        <!--  文件解锁结果控件  -->
        <Grid
            Grid.Row="2"
            Margin="30,0,30,3"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="{ThemeResource ControlCornerRadius}">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                Grid.Row="0"
                Height="35"
                Padding="6,0,0,0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="65" />
                </Grid.ColumnDefinitions>

                <TextBlock
                    x:Name="FileFolderNameHeaderTextBlock"
                    Grid.Column="0"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderName')}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap">

                    <ToolTipService.ToolTip>
                        <ToolTip ToolTipService.ToolTip="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderName')}" Visibility="{x:Bind FileFolderNameHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                    </ToolTipService.ToolTip>
                </TextBlock>

                <TextBlock
                    x:Name="FileFolderTypeHeaderTextBlock"
                    Grid.Column="1"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderType')}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap">

                    <ToolTipService.ToolTip>
                        <ToolTip ToolTipService.ToolTip="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderType')}" Visibility="{x:Bind FileFolderTypeHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                    </ToolTipService.ToolTip>
                </TextBlock>

                <TextBlock
                    x:Name="FileFolderAmountHeaderTextBlock"
                    Grid.Column="2"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderAmount')}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap">

                    <ToolTipService.ToolTip>
                        <ToolTip>
                            <StackPanel>
                                <TextBlock Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderAmount')}" />
                                <TextBlock Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileAmount')}" />
                                <TextBlock Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FolderAmount')}" />
                            </StackPanel>
                        </ToolTip>
                    </ToolTipService.ToolTip>
                </TextBlock>

                <TextBlock
                    x:Name="FileFolderStatusHeaderTextBlock"
                    Grid.Column="3"
                    Margin="10,0,0,0"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderStatus')}"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="NoWrap">

                    <ToolTipService.ToolTip>
                        <ToolTip Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderStatus')}" Visibility="{x:Bind FileFolderStatusHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                    </ToolTipService.ToolTip>
                </TextBlock>
            </Grid>

            <ListView
                Grid.Row="1"
                Margin="0,0,0,3"
                ItemsSource="{x:Bind FileUnlockCollection}"
                SelectionMode="None">

                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource DefaultListViewItemStyle}" TargetType="ListViewItem">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </ListView.ItemContainerStyle>

                <ItemsControl.ItemContainerTransitions>
                    <TransitionCollection />
                </ItemsControl.ItemContainerTransitions>

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:FileUnlockModel">
                        <Grid
                            Height="35"
                            Padding="6,0,0,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="65" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                x:Name="FileFolderNameTextBlock"
                                Grid.Column="0"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind FileFolderName}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind FileFolderPath, Mode=OneWay}"
                                        Placement="Mouse"
                                        Visibility="{x:Bind converter:ValueConverterHelper.StringToVisibilityConvert(FileFolderPath), Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FileFolderTypeTextBlock"
                                Grid.Column="1"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind FileFolderType}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind FileFolderType}"
                                        Placement="Mouse"
                                        Visibility="{Binding ElementName=FileFolderTypeTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FileFolderAmountTextBlock"
                                Grid.Column="2"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind FileFolderAmount}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind FileFolderAmount}"
                                        Placement="Mouse"
                                        Visibility="{Binding ElementName=FileFolderAmountTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FileFolderNotStartedTextBlock"
                                Grid.Column="3"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderNotStarted')}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap"
                                Visibility="{x:Bind converter:ValueCheckConverterHelper.CheckFileUnlockStateVisibility(FileUnlockState, enum:FileUnlockState.NotStarted), Mode=OneWay}">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderNotStarted')}"
                                        Placement="Mouse"
                                        Visibility="{Binding ElementName=FileFolderNotStartedTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <StackPanel
                                Grid.Column="3"
                                Margin="10,0,0,0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Visibility="{x:Bind converter:ValueCheckConverterHelper.CheckFileUnlockStateVisibility(FileUnlockState, enum:FileUnlockState.Processing), Mode=OneWay}">

                                <ProgressRing
                                    Width="14"
                                    Height="14"
                                    Margin="0,0,6,0"
                                    IsActive="{x:Bind x:Object.Equals(FileUnlockState, enum:FileUnlockState.Processing), Mode=OneWay}"
                                    IsIndeterminate="False"
                                    Value="{x:Bind FileUnlockProgressingPercentage, Mode=OneWay}" />

                                <TextBlock
                                    x:Name="FileFolderProcessingTextBlock"
                                    Grid.Column="3"
                                    MinWidth="50"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap">

                                    <Run Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderProcessing')}" />
                                    <Run Text="{x:Bind sys:Convert.ToString(FileUnlockFinishedCount), Mode=OneWay}" />
                                    <Run Text="/" />
                                    <Run Text="{x:Bind sys:Convert.ToString(SubFileList.Count)}" />

                                    <ToolTipService.ToolTip>
                                        <ToolTip Placement="Mouse">
                                            <TextBlock>
                                                <Run Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderProcessing')}" />
                                                <Run Text="{x:Bind sys:Convert.ToString(FileUnlockFinishedCount), Mode=OneWay}" />
                                                <Run Text="/" />
                                                <Run Text="{x:Bind sys:Convert.ToString(SubFileList.Count)}" />
                                            </TextBlock>
                                        </ToolTip>
                                    </ToolTipService.ToolTip>
                                </TextBlock>
                            </StackPanel>

                            <TextBlock
                                x:Name="FileFolderCancelledTextBlock"
                                Grid.Column="3"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderCancelled')}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap"
                                Visibility="{x:Bind converter:ValueCheckConverterHelper.CheckFileUnlockStateVisibility(FileUnlockState, enum:FileUnlockState.Cancelled), Mode=OneWay}">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderCancelled')}"
                                        Placement="Mouse"
                                        Visibility="{Binding ElementName=FileFolderCancelledTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FileFolderFailedTextBlock"
                                Grid.Column="3"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderFailed')}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap"
                                Visibility="{x:Bind converter:ValueCheckConverterHelper.CheckFileUnlockStateVisibility(FileUnlockState, enum:FileUnlockState.Failed), Mode=OneWay}">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderFailed')}"
                                        Placement="Mouse"
                                        Visibility="{Binding ElementName=FileFolderFailedTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FileFolderSuccessfullyTextBlock"
                                Grid.Column="3"
                                MinWidth="50"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                FontSize="14"
                                Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderSuccessfully')}"
                                TextAlignment="Left"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap"
                                Visibility="{x:Bind converter:ValueCheckConverterHelper.CheckFileUnlockStateVisibility(FileUnlockState, enum:FileUnlockState.Successfully), Mode=OneWay}">

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('FileFolderSuccessfully')}"
                                        Placement="Mouse"
                                        Visibility="{Binding ElementName=FileFolderSuccessfullyTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <StackPanel
                                Grid.Column="4"
                                Margin="10,0,0,0"
                                Orientation="Horizontal">

                                <Button
                                    Width="30"
                                    Height="30"
                                    Padding="2"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    Command="{StaticResource DeleteCommand}"
                                    CommandParameter="{x:Bind}"
                                    IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsModifyingNow), Mode=OneWay}"
                                    Style="{StaticResource AlternateCloseButtonStyle}">

                                    <FontIcon
                                        Margin="4"
                                        FontSize="16"
                                        Glyph="&#xE711;" />

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('Delete')}" Placement="Mouse" />
                                    </ToolTipService.ToolTip>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock
                Grid.Row="1"
                Margin="0,10,0,0"
                VerticalAlignment="Top"
                Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('EmptyList')}"
                TextAlignment="Center"
                Visibility="{x:Bind converter:ValueConverterHelper.IntToVisibilityReverseConvert(FileUnlockCollection.Count), Mode=OneWay}" />
        </Grid>

        <!--  文件解锁控制栏  -->
        <Grid
            Grid.Row="3"
            Margin="30,0,30,3"
            ColumnSpacing="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Click="{x:Bind OnSelectFileClicked}"
                Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('SelectFile')}"
                IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsModifyingNow), Mode=OneWay}" />

            <Button
                Grid.Column="1"
                Click="{x:Bind OnSelectFolderClicked}"
                Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('SelectFolder')}"
                IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsModifyingNow), Mode=OneWay}" />

            <Button
                Grid.Column="2"
                Click="{x:Bind OnClearListClicked}"
                Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('ClearList')}"
                IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsModifyingNow), Mode=OneWay}" />

            <Button
                Grid.Column="3"
                Click="{x:Bind OnOpenTaskManagerClicked}"
                Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('OpenTaskManager')}" />

            <TextBlock
                Grid.Column="4"
                VerticalAlignment="Center"
                Text="{x:Bind x:String.Format(TotalString, FileUnlockCollection.Count), Mode=OneWay}"
                TextAlignment="Center" />

            <HyperlinkButton
                Grid.Column="5"
                Margin="0,0,10,0"
                Click="{x:Bind OnViewErrorInformationClicked}"
                Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('ViewErrorInformation')}"
                Visibility="{x:Bind IsOperationFailed, Mode=OneWay}" />

            <ProgressRing
                Grid.Column="6"
                Width="25"
                Height="25"
                Margin="0,0,5,0"
                VerticalAlignment="Center"
                IsActive="True"
                IsIndeterminate="True"
                Visibility="{x:Bind IsModifyingNow, Mode=OneWay}" />

            <TextBlock
                Grid.Column="7"
                Margin="0,0,5,0"
                VerticalAlignment="Center"
                Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('ModifyingNow')}"
                Visibility="{x:Bind IsModifyingNow, Mode=OneWay}" />

            <Button
                Grid.Column="8"
                Click="{x:Bind OnUnlockClicked}"
                Content="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('Unlock')}"
                IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsModifyingNow), Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}"
                Visibility="{x:Bind converter:ValueConverterHelper.BooleanToVisibilityReverseConvert(IsModifyingNow), Mode=OneWay}" />

            <Button
                Grid.Column="8"
                Click="{x:Bind OnCancelUnlockClicked}"
                IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsOperationCancelling), Mode=OneWay}"
                Style="{StaticResource AccentButtonStyle}"
                Visibility="{x:Bind IsModifyingNow, Mode=OneWay}">

                <Grid>
                    <TextBlock Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('CancelUnlock')}" Visibility="{x:Bind converter:ValueConverterHelper.BooleanToVisibilityReverseConvert(IsOperationCancelling), Mode=OneWay}" />
                    <TextBlock Text="{x:Bind rootservice:ResourceService.FileUnlockResource.GetString('Cancelling')}" Visibility="{x:Bind IsOperationCancelling, Mode=OneWay}" />
                </Grid>
            </Button>
        </Grid>
    </Grid>
</Page>

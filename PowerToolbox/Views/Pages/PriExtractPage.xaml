<Page
    x:Class="PowerToolbox.Views.Pages.PriExtractPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:class="using:PowerToolbox.Extensions.DataType.Class"
    xmlns:converter="using:PowerToolbox.Helpers.Converters"
    xmlns:customcontrol="using:PowerToolbox.Views.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:PowerToolbox.Models"
    xmlns:rootservice="using:PowerToolbox.Services.Root"
    xmlns:system="using:System"
    AllowDrop="True"
    NavigationCacheMode="Enabled"
    mc:Ignorable="d">

    <Page.Resources>
        <CornerRadius x:Key="ListViewItemCornerRadius">0</CornerRadius>
        <SolidColorBrush x:Key="HyperlinkButtonBackgroundPointerOver" Color="Transparent" />
        <SolidColorBrush x:Key="HyperlinkButtonBackgroundPressed" Color="Transparent" />
        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="Transparent" />
        <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="Transparent" />

        <class:ExecuteCommand x:Name="StringCommand" ExecuteRequested="{x:Bind OnStringExecuteRequested}" />
        <class:ExecuteCommand x:Name="FilePathCommand" ExecuteRequested="{x:Bind OnFilePathExecuteRequested}" />
        <class:ExecuteCommand x:Name="EmbeddedDataCommand" ExecuteRequested="{x:Bind OnEmbeddedDataExecuteRequested}" />

        <Style x:Key="ListViewItemListStyle" TargetType="ListViewItem">
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}" />
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="Foreground" Value="{ThemeResource ListViewItemForeground}" />
            <Setter Property="TabNavigation" Value="Local" />
            <Setter Property="IsHoldingEnabled" Value="True" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="MinWidth" Value="{ThemeResource ListViewItemMinWidth}" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="AllowDrop" Value="False" />
            <Setter Property="UseSystemFocusVisuals" Value="True" />
            <Setter Property="FocusVisualMargin" Value="1" />

            <Setter Property="FocusVisualPrimaryBrush" Value="{ThemeResource ListViewItemFocusVisualPrimaryBrush}" />
            <Setter Property="FocusVisualPrimaryThickness" Value="2" />
            <Setter Property="FocusVisualSecondaryBrush" Value="{ThemeResource ListViewItemFocusVisualSecondaryBrush}" />
            <Setter Property="FocusVisualSecondaryThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <customcontrol:BorderedListViewItemPresenter
                            x:Name="Root"
                            HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1"
                            CheckBoxBorderBrush="{ThemeResource ListViewItemCheckBoxBorderBrush}"
                            CheckBoxBrush="{ThemeResource ListViewItemCheckBoxBrush}"
                            CheckBoxCornerRadius="{ThemeResource ListViewItemCheckBoxCornerRadius}"
                            CheckBoxDisabledBorderBrush="{ThemeResource ListViewItemCheckBoxDisabledBorderBrush}"
                            CheckBoxDisabledBrush="{ThemeResource ListViewItemCheckBoxDisabledBrush}"
                            CheckBoxPointerOverBorderBrush="{ThemeResource ListViewItemCheckBoxPointerOverBorderBrush}"
                            CheckBoxPointerOverBrush="{ThemeResource ControlAltFillColorTertiaryBrush}"
                            CheckBoxPressedBorderBrush="{ThemeResource ListViewItemCheckBoxPressedBorderBrush}"
                            CheckBoxPressedBrush="{ThemeResource ListViewItemCheckBoxPressedBrush}"
                            CheckBoxSelectedBrush="{ThemeResource ListViewItemCheckBoxSelectedBrush}"
                            CheckBoxSelectedDisabledBrush="{ThemeResource ListViewItemCheckBoxSelectedDisabledBrush}"
                            CheckBoxSelectedPointerOverBrush="{ThemeResource ListViewItemCheckBoxSelectedPointerOverBrush}"
                            CheckBoxSelectedPressedBrush="{ThemeResource ListViewItemCheckBoxSelectedPressedBrush}"
                            CheckBrush="{ThemeResource ListViewItemCheckBrush}"
                            CheckDisabledBrush="{ThemeResource ListViewItemCheckDisabledBrush}"
                            CheckMode="{ThemeResource ListViewItemCheckMode}"
                            CheckPressedBrush="{ThemeResource ListViewItemCheckPressedBrush}"
                            ContentMargin="{TemplateBinding Padding}"
                            ContentTransitions="{TemplateBinding ContentTransitions}"
                            Control.IsTemplateFocusTarget="True"
                            CornerRadius="0"
                            DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}"
                            DragBackground="{ThemeResource ListViewItemDragBackground}"
                            DragForeground="{ThemeResource ListViewItemDragForeground}"
                            DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}"
                            FocusBorderBrush="{ThemeResource ListViewItemFocusBorderBrush}"
                            FocusSecondaryBorderBrush="{ThemeResource ListViewItemFocusSecondaryBorderBrush}"
                            FocusVisualMargin="{TemplateBinding FocusVisualMargin}"
                            FocusVisualPrimaryBrush="{TemplateBinding FocusVisualPrimaryBrush}"
                            FocusVisualPrimaryThickness="{TemplateBinding FocusVisualPrimaryThickness}"
                            FocusVisualSecondaryBrush="{TemplateBinding FocusVisualSecondaryBrush}"
                            FocusVisualSecondaryThickness="{TemplateBinding FocusVisualSecondaryThickness}"
                            PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackground}"
                            PointerOverBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            PointerOverForeground="{ThemeResource ListViewItemForegroundPointerOver}"
                            PressedBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}"
                            SelectedBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            SelectedDisabledBackground="{ThemeResource ListViewItemBackgroundSelectedDisabled}"
                            SelectedForeground="{ThemeResource ListViewItemForegroundSelected}"
                            SelectedPointerOverBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            SelectedPressedBackground="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            SelectionCheckMarkVisualEnabled="{ThemeResource ListViewItemSelectionCheckMarkVisualEnabled}"
                            SelectionIndicatorBrush="{ThemeResource ListViewItemSelectionIndicatorBrush}"
                            SelectionIndicatorCornerRadius="{ThemeResource ListViewItemSelectionIndicatorCornerRadius}"
                            SelectionIndicatorDisabledBrush="{ThemeResource ListViewItemSelectionIndicatorDisabledBrush}"
                            SelectionIndicatorPointerOverBrush="{ThemeResource ListViewItemSelectionIndicatorPointerOverBrush}"
                            SelectionIndicatorPressedBrush="{ThemeResource ListViewItemSelectionIndicatorPressedBrush}"
                            SelectionIndicatorVisualEnabled="{ThemeResource ListViewItemSelectionIndicatorVisualEnabled}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="30,15,30,0"
            FontSize="22"
            Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('PriExtract')}" />

        <TextBlock
            x:Name="GetResultsTextBlock"
            Grid.Row="1"
            Margin="32,3,32,3"
            FontSize="14"
            Text="{x:Bind GetResults, Mode=OneWay}"
            TextTrimming="CharacterEllipsis"
            TextWrapping="NoWrap">

            <ToolTipService.ToolTip>
                <ToolTip Content="{x:Bind GetResults, Mode=OneWay}" Visibility="{x:Bind GetResultsTextBlock.IsTextTrimmed, Mode=OneWay}" />
            </ToolTipService.ToolTip>
        </TextBlock>

        <Grid Grid.Row="2" Margin="30,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  包资源索引列表控件  -->
            <Grid
                Grid.Column="0"
                Margin="0,0,0,5"
                Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource ControlCornerRadius}">

                <ListView
                    x:Name="PriExtractStringListView"
                    Margin="0,0,0,3"
                    IsItemClickEnabled="True"
                    ItemClick="{x:Bind OnStringItemClicked}"
                    ItemContainerStyle="{StaticResource ListViewItemListStyle}"
                    ItemsSource="{x:Bind StringCollection}"
                    SelectionMode="Multiple"
                    Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[0].SelectedValue), Mode=OneWay}">

                    <ItemsControl.ItemContainerTransitions>
                        <TransitionCollection />
                    </ItemsControl.ItemContainerTransitions>

                    <ListView.Header>
                        <Grid
                            Grid.Row="0"
                            Height="35"
                            Padding="34,0,0,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1"
                            Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[0].SelectedValue), Mode=OneWay}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                x:Name="StringKeyHeaderTextBlock"
                                Grid.Column="0"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Key')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Key')}" Visibility="{x:Bind StringKeyHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="StringLanguageHeaderTextBlock"
                                Grid.Column="1"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Language')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Language')}" Visibility="{x:Bind StringLanguageHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="StringContentHeaderTextBlock"
                                Grid.Column="2"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Content')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Content')}" Visibility="{x:Bind StringContentHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="StringOperationHeaderTextBlock"
                                Grid.Column="3"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Operation')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Operation')}" Visibility="{x:Bind StringOperationHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>
                        </Grid>
                    </ListView.Header>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="model:StringModel">
                            <Grid
                                Height="35"
                                Padding="6,0,0,0"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="100" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="70" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    x:Name="StringKeyTextBlock"
                                    Grid.Column="0"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{x:Bind Key}"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap"
                                    ToolTipService.ToolTip="{x:Bind Key}" />

                                <TextBlock
                                    x:Name="StringLanguageTextBlock"
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{x:Bind Language}"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind Language}" Visibility="{Binding ElementName=StringLanguageTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                    </ToolTipService.ToolTip>
                                </TextBlock>

                                <TextBlock
                                    x:Name="StringContentTextBlock"
                                    Grid.Column="2"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{x:Bind Content}"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind Content}" Visibility="{Binding ElementName=StringContentTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                    </ToolTipService.ToolTip>
                                </TextBlock>

                                <HyperlinkButton
                                    Grid.Column="3"
                                    Margin="10,0,0,0"
                                    Padding="0"
                                    VerticalAlignment="Center"
                                    Command="{StaticResource StringCommand}"
                                    CommandParameter="{x:Bind}"
                                    Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Copy')}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid
                    Margin="0,45,0,0"
                    VerticalAlignment="Top"
                    Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[0].SelectedValue), Mode=OneWay}">

                    <TextBlock
                        Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('EmptyList')}"
                        TextAlignment="Center"
                        Visibility="{x:Bind converter:ValueConverterHelper.IntToVisibilityReverseConvert(StringCollection.Count), Mode=OneWay}" />
                </Grid>

                <ListView
                    x:Name="PriExtractFilePathListView"
                    Margin="0,0,0,3"
                    IsItemClickEnabled="True"
                    ItemClick="{x:Bind OnFilePathItemClicked}"
                    ItemContainerStyle="{StaticResource ListViewItemListStyle}"
                    ItemsSource="{x:Bind FilePathCollection}"
                    SelectionMode="Multiple"
                    Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[1].SelectedValue), Mode=OneWay}">

                    <ItemsControl.ItemContainerTransitions>
                        <TransitionCollection />
                    </ItemsControl.ItemContainerTransitions>

                    <ListView.Header>
                        <!--  文件路径资源  -->
                        <Grid
                            Grid.Row="0"
                            Height="35"
                            Padding="34,0,0,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1"
                            Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[1].SelectedValue), Mode=OneWay}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                x:Name="FilePathKeyHeaderTextBlock"
                                Grid.Column="0"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Key')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Key')}" Visibility="{x:Bind FilePathKeyHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FilePathAbsolutePathHeaderTextBlock"
                                Grid.Column="1"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('AbsolutePath')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('AbsolutePath')}" Visibility="{x:Bind FilePathAbsolutePathHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="FilePathOperationHeaderTextBlock"
                                Grid.Column="2"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Operation')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Operation')}" Visibility="{x:Bind FilePathOperationHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>
                        </Grid>
                    </ListView.Header>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="model:FilePathModel">
                            <Grid
                                Height="35"
                                Padding="6,0,0,0"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="70" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    x:Name="FilePathKeyTextBlock"
                                    Grid.Column="0"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{x:Bind Key}"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind Key}" Visibility="{Binding ElementName=FilePathKeyTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                    </ToolTipService.ToolTip>
                                </TextBlock>

                                <TextBlock
                                    x:Name="FilePathAbsolutePathTextBlock"
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{x:Bind AbsolutePath}"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind AbsolutePath}" Visibility="{Binding ElementName=FilePathAbsolutePathTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                    </ToolTipService.ToolTip>
                                </TextBlock>

                                <HyperlinkButton
                                    Grid.Column="2"
                                    Margin="10,0,0,0"
                                    Padding="0"
                                    VerticalAlignment="Center"
                                    Command="{StaticResource FilePathCommand}"
                                    CommandParameter="{x:Bind}"
                                    Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Copy')}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid
                    Margin="0,45,0,0"
                    VerticalAlignment="Top"
                    Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[1].SelectedValue), Mode=OneWay}">

                    <TextBlock
                        Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('EmptyList')}"
                        TextAlignment="Center"
                        Visibility="{x:Bind converter:ValueConverterHelper.IntToVisibilityReverseConvert(FilePathCollection.Count), Mode=OneWay}" />
                </Grid>

                <ListView
                    x:Name="PriExtractEmbeddedDataListView"
                    IsItemClickEnabled="True"
                    ItemClick="{x:Bind OnEmbeddedDataItemClicked}"
                    ItemContainerStyle="{StaticResource ListViewItemListStyle}"
                    ItemsSource="{x:Bind EmbeddedDataCollection}"
                    SelectionMode="Multiple"
                    Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[2].SelectedValue), Mode=OneWay}">

                    <ItemsControl.ItemContainerTransitions>
                        <TransitionCollection />
                    </ItemsControl.ItemContainerTransitions>

                    <ListView.Header>
                        <!--  嵌入数据资源  -->
                        <Grid
                            Grid.Row="0"
                            Height="35"
                            Padding="34,0,0,0"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="0,0,0,1"
                            Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[2].SelectedValue), Mode=OneWay}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="70" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                x:Name="EmbeddedDataKeyHeaderTextBlock"
                                Grid.Column="0"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Key')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Key')}" Visibility="{x:Bind EmbeddedDataKeyHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>

                            <TextBlock
                                x:Name="EmbeddedDataOperationHeaderTextBlock"
                                Grid.Column="1"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Operation')}"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap">

                                <ToolTipService.ToolTip>
                                    <ToolTip Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Operation')}" Visibility="{x:Bind EmbeddedDataOperationHeaderTextBlock.IsTextTrimmed, Mode=OneWay}" />
                                </ToolTipService.ToolTip>
                            </TextBlock>
                        </Grid>
                    </ListView.Header>

                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="model:EmbeddedDataModel">
                            <Grid
                                Height="35"
                                Padding="6,0,0,0"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="0,0,0,1">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="70" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    x:Name="EmbeddedDataKeyTextBlock"
                                    Grid.Column="0"
                                    Margin="10,0,0,0"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Center"
                                    FontSize="14"
                                    Text="{x:Bind Key}"
                                    TextAlignment="Left"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap">

                                    <ToolTipService.ToolTip>
                                        <ToolTip Content="{x:Bind Key}" Visibility="{Binding ElementName=EmbeddedDataKeyTextBlock, Path=IsTextTrimmed, Mode=OneWay}" />
                                    </ToolTipService.ToolTip>
                                </TextBlock>

                                <HyperlinkButton
                                    Grid.Column="1"
                                    Margin="10,0,0,0"
                                    Padding="0"
                                    VerticalAlignment="Center"
                                    Command="{StaticResource EmbeddedDataCommand}"
                                    CommandParameter="{x:Bind}"
                                    Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('Extract')}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Grid
                    Margin="0,45,0,0"
                    VerticalAlignment="Top"
                    Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[2].SelectedValue), Mode=OneWay}">

                    <TextBlock
                        Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('EmptyList')}"
                        TextAlignment="Center"
                        Visibility="{x:Bind converter:ValueConverterHelper.IntToVisibilityReverseConvert(EmbeddedDataCollection.Count), Mode=OneWay}" />
                </Grid>
            </Grid>

            <AppBarSeparator Grid.Column="1" Margin="10,-6" />

            <!--  包资源索引控制栏  -->
            <Grid
                Grid.Column="2"
                Width="260"
                Margin="0,-4,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">

                    <TextBlock
                        Margin="0,0,0,3"
                        FontSize="16"
                        Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExtractOptions')}" />

                    <CheckBox
                        Click="{x:Bind OnExtractSaveSameClicked}"
                        Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExtractSaveSame')}"
                        IsChecked="{x:Bind IsExtractSaveSame, Mode=TwoWay}" />

                    <CheckBox
                        Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExtractSaveString')}"
                        IsChecked="{x:Bind IsExtractSaveString, Mode=TwoWay}"
                        IsEnabled="{x:Bind IsExtractSaveSame, Mode=OneWay}" />

                    <CheckBox
                        Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExtractSaveFilePath')}"
                        IsChecked="{x:Bind IsExtractSaveFilePath, Mode=TwoWay}"
                        IsEnabled="{x:Bind IsExtractSaveSame, Mode=OneWay}" />

                    <CheckBox
                        Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExtractSaveEmbeddedData')}"
                        IsChecked="{x:Bind IsExtractSaveEmbeddedData, Mode=TwoWay}"
                        IsEnabled="{x:Bind IsExtractSaveSame, Mode=OneWay}" />

                    <MenuFlyoutSeparator Grid.Row="5" Margin="5,3" />

                    <DropDownButton
                        Grid.Row="2"
                        Margin="0,0,0,3"
                        HorizontalAlignment="Stretch"
                        Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('MultiSelect')}">

                        <DropDownButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <MenuFlyout.MenuFlyoutPresenterStyle>
                                    <Style BasedOn="{StaticResource DefaultMenuFlyoutPresenterStyle}" TargetType="MenuFlyoutPresenter">
                                        <Setter Property="MinWidth" Value="260" />
                                    </Style>
                                </MenuFlyout.MenuFlyoutPresenterStyle>

                                <MenuFlyoutItem Click="{x:Bind OnSelectAllClicked}" Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('SelectAll')}">

                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE8B3;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>

                                <MenuFlyoutItem Click="{x:Bind OnSelectNoneClicked}" Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('SelectNone')}">

                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xE8E6;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>

                                <MenuFlyoutItem Click="{x:Bind OnSelectReverseClicked}" Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('SelectReverse')}">

                                    <MenuFlyoutItem.Icon>
                                        <FontIcon Glyph="&#xEA98;" />
                                    </MenuFlyoutItem.Icon>
                                </MenuFlyoutItem>
                            </MenuFlyout>
                        </DropDownButton.Flyout>
                    </DropDownButton>

                    <Button
                        HorizontalAlignment="Stretch"
                        Click="{x:Bind OnSelectSaveFolderClicked}"
                        Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('SelectSaveFolder')}"
                        IsEnabled="{x:Bind IsExtractSaveSame, Mode=OneWay}"
                        ToolTipService.ToolTip="{x:Bind x:String.Format(SelectedFolderString, SelectedSaveFolder), Mode=OneWay}" />

                    <MenuFlyoutSeparator Margin="5,3" />

                    <ContentControl HorizontalContentAlignment="Stretch" IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsProcessing), Mode=OneWay}">
                        <StackPanel>
                            <Button
                                Margin="0,0,0,3"
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnSelectFileClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('SelectFile')}" />

                            <ComboBox
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(LanguageCollection.Count), Mode=OneWay}"
                                ItemsSource="{x:Bind LanguageCollection}"
                                PlaceholderText="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('NoAvailableLanguage')}"
                                SelectedItem="{x:Bind SelectedLanguage, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnLanguageSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind, ResourceCandidateKindList[0]), Mode=OneWay}" />

                            <MenuFlyoutSeparator Margin="5,3,5,3" />

                            <ComboBox
                                Margin="0,0,0,3"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Center"
                                DisplayMemberPath="DisplayMember"
                                ItemsSource="{x:Bind ResourceCandidateKindList}"
                                SelectedItem="{x:Bind SelectedResourceCandidateKind, Mode=OneWay}"
                                SelectionChanged="{x:Bind OnResourceCandidateKindSelectionChanged}"
                                Style="{StaticResource ComboBoxStyle}" />

                            <AutoSuggestBox
                                Margin="0,0,0,3"
                                IsEnabled="{x:Bind HasStringResource, Mode=OneWay}"
                                PlaceholderText="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('StringPlaceHolderText')}"
                                QueryIcon="Find"
                                QuerySubmitted="{x:Bind OnStringQuerySubmitted}"
                                Text="{x:Bind StringSearchText, Mode=OneWay}"
                                TextChanged="{x:Bind OnStringTextChanged}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[0].SelectedValue), Mode=OneWay}" />

                            <AutoSuggestBox
                                Margin="0,0,0,3"
                                IsEnabled="{x:Bind HasFilePathResource, Mode=OneWay}"
                                PlaceholderText="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('FilePathPlaceHolderText')}"
                                QueryIcon="Find"
                                QuerySubmitted="{x:Bind OnFilePathQuerySubmitted}"
                                Text="{x:Bind FilePathSearchText, Mode=OneWay}"
                                TextChanged="{x:Bind OnFilePathTextChanged}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[1].SelectedValue), Mode=OneWay}" />

                            <AutoSuggestBox
                                Margin="0,0,0,3"
                                IsEnabled="{x:Bind HasEmbeddedDataResource, Mode=OneWay}"
                                PlaceholderText="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('EmbeddedDataPlaceHolderText')}"
                                QueryIcon="Find"
                                QuerySubmitted="{x:Bind OnEmbeddedDataQuerySubmitted}"
                                Text="{x:Bind EmbeddedDataSearchText, Mode=OneWay}"
                                TextChanged="{x:Bind OnEmbeddedDataTextChanged}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[2].SelectedValue), Mode=OneWay}" />

                            <Button
                                Margin="0,0,0,3"
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnCopySelectedStringClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('CopySelectedString')}"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(StringCollection.Count), Mode=OneWay}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[0].SelectedValue), Mode=OneWay}" />

                            <Button
                                Margin="0,0,0,3"
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnCopySelectedFilePathClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('CopySelectedFilePath')}"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(FilePathCollection.Count), Mode=OneWay}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[1].SelectedValue), Mode=OneWay}" />

                            <Button
                                Margin="0,0,0,3"
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnExportSelectedEmbeddedDataClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExportSelectedEmbeddedData')}"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(EmbeddedDataCollection.Count), Mode=OneWay}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[2].SelectedValue), Mode=OneWay}" />

                            <Button
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnCopyAllStringClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('CopyAllString')}"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(StringCollection.Count), Mode=OneWay}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[0].SelectedValue), Mode=OneWay}" />

                            <Button
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnCopyAllFilePathClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('CopyAllFilePath')}"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(FilePathCollection.Count), Mode=OneWay}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[1].SelectedValue), Mode=OneWay}" />

                            <Button
                                HorizontalAlignment="Stretch"
                                Click="{x:Bind OnExportAllEmbeddedDataClicked}"
                                Content="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ExportAllEmbeddedData')}"
                                IsEnabled="{x:Bind system:Convert.ToBoolean(EmbeddedDataCollection.Count), Mode=OneWay}"
                                Visibility="{x:Bind converter:ValueConverterHelper.ObjectToVisibilityConvert(SelectedResourceCandidateKind.SelectedValue, ResourceCandidateKindList[2].SelectedValue), Mode=OneWay}" />
                        </StackPanel>
                    </ContentControl>
                </StackPanel>

                <StackPanel
                    Grid.Row="1"
                    Margin="0,5,0,5"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Orientation="Horizontal"
                    Spacing="5"
                    Visibility="{x:Bind IsProcessing, Mode=OneWay}">
                    <ProgressRing
                        Width="20"
                        Height="20"
                        IsActive="{x:Bind IsProcessing, Mode=OneWay}" />

                    <TextBlock Text="{x:Bind rootservice:ResourceService.PriExtractResource.GetString('ProcessingNow')}" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>

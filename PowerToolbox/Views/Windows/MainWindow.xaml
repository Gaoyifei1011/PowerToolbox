<Window
    x:Class="PowerToolbox.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    xmlns:converter="using:PowerToolbox.Helpers.Converters"
    xmlns:rootservice="using:PowerToolbox.Services.Root"
    xmlns:settingsservice="using:PowerToolbox.Services.Settings"
    Title="{x:Bind WindowTitle, Mode=OneWay}"
    SystemBackdrop="{x:Bind WindowSystemBackdrop, Mode=OneWay}">

    <Page
        x:Name="MainPage"
        ActualThemeChanged="{x:Bind OnActualThemeChanged}"
        FlowDirection="{x:Bind settingsservice:LanguageService.FlowDirection}"
        KeyDown="{x:Bind OnKeyDown}"
        RequestedTheme="{x:Bind WindowTheme, Mode=OneWay}">

        <Page.Resources>
            <ResourceDictionary>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />

                <MenuFlyout x:Name="TitlebarMenuFlyout" Placement="BottomEdgeAlignedLeft">

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnRestoreClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind IsWindowMaximized, Mode=OneWay}"
                        Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Restore')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE923;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMoveClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsWindowMaximized), Mode=OneWay}"
                        Tag="{x:Bind TitlebarMenuFlyout}"
                        Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Move')}" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnSizeClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsWindowMaximized), Mode=OneWay}"
                        Tag="{x:Bind TitlebarMenuFlyout}"
                        Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Size')}" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMinimizeClicked}"
                        FontSize="12"
                        Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Minimize')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE921;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnMaximizeClicked}"
                        FontSize="12"
                        IsEnabled="{x:Bind converter:ValueConverterHelper.BooleanReverseConvert(IsWindowMaximized), Mode=OneWay}"
                        Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Maximize')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE922;" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutSeparator Width="200" />

                    <MenuFlyoutItem
                        Height="28"
                        Padding="11,0"
                        Click="{x:Bind OnCloseClicked}"
                        FontSize="12"
                        Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Close')}">

                        <MenuFlyoutItem.Icon>
                            <FontIcon Margin="6" Glyph="&#xE8BB;" />
                        </MenuFlyoutItem.Icon>

                        <MenuFlyoutItem.KeyboardAccelerators>
                            <KeyboardAccelerator Key="F4" Modifiers="Menu" />
                        </MenuFlyoutItem.KeyboardAccelerators>
                    </MenuFlyoutItem>
                </MenuFlyout>
            </ResourceDictionary>
        </Page.Resources>

        <Grid>
            <!--  应用标题栏内容  -->
            <TitleBar
                x:Name="AppTitlebar"
                Title="{x:Bind WindowTitle, Mode=OneWay}"
                Height="48"
                VerticalAlignment="Top">

                <TitleBar.LeftHeader>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            Grid.Column="0"
                            Width="40"
                            Height="36"
                            Margin="4,0,0,0"
                            VerticalAlignment="Center"
                            Background="{ThemeResource TitleBarBackButtonBackground}"
                            Click="{x:Bind OnBackClicked}"
                            IsEnabled="{x:Bind IsBackEnabled, Mode=OneWay}"
                            Style="{StaticResource SubtleButtonStyle}">

                            <AnimatedIcon
                                Width="16"
                                Height="16"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                AnimatedIcon.State="Normal">
                                <animatedvisuals:AnimatedBackVisualSource />
                                <AnimatedIcon.FallbackIconSource>
                                    <FontIconSource
                                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                        FontSize="16"
                                        MirroredWhenRightToLeft="True" />
                                </AnimatedIcon.FallbackIconSource>
                            </AnimatedIcon>

                            <ToolTipService.ToolTip>
                                <ToolTip
                                    MaxWidth="9999"
                                    Padding="0"
                                    Background="Transparent">

                                    <Grid>
                                        <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                        <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Back')}" />
                                    </Grid>
                                </ToolTip>
                            </ToolTipService.ToolTip>
                        </Button>

                        <ImageIcon
                            Grid.Column="1"
                            MaxWidth="16"
                            MaxHeight="16"
                            Margin="2,0,2,0"
                            Source="ms-appx:///Assets/Square44x44Logo.altform-unplated_targetsize-256.png" />
                    </Grid>
                </TitleBar.LeftHeader>
            </TitleBar>

            <NavigationView
                x:Name="MainNavigationView"
                Margin="0,40,0,0"
                AlwaysShowHeader="False"
                Canvas.ZIndex="0"
                Collapsed="{x:Bind OnCollapsed}"
                Expanding="{x:Bind OnExpanding}"
                IsBackButtonVisible="Collapsed"
                IsPaneToggleButtonVisible="False"
                IsSettingsVisible="False"
                ItemInvoked="{x:Bind OnItemInvoked}"
                Loaded="{x:Bind OnLoaded}"
                OpenPaneLength="220"
                PaneDisplayMode="Left"
                SelectedItem="{x:Bind SelectedItem, Mode=OneWay}">

                <NavigationView.MenuItems>
                    <!--  所有工具  -->
                    <NavigationViewItem Tag="{x:Bind PageList[0].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/AllTools.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowAllToolsText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('AllTools')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowAllToolsText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('AllTools')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>
                    </NavigationViewItem>

                    <NavigationViewItemSeparator Margin="0,0,6,0" />

                    <!--  休闲工具  -->
                    <NavigationViewItem Tag="{x:Bind PageList[1].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/Relaxation.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowRelaxationText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Relaxation')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowRelaxationText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Relaxation')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>

                        <NavigationViewItem.MenuItems>
                            <NavigationViewItem Tag="{x:Bind PageList[2].Key}">

                                <TextBlock
                                    x:Name="WindowLoafText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Loaf')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/Loaf.png" />
                                </NavigationViewItem.Icon>

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowLoafText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Loaf')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>
                        </NavigationViewItem.MenuItems>
                    </NavigationViewItem>

                    <!--  文件工具  -->
                    <NavigationViewItem Tag="{x:Bind PageList[3].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/File.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowFileText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('File')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowFileText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('File')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>

                        <NavigationViewItem.MenuItems>
                            <NavigationViewItem Tag="{x:Bind PageList[4].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/FileManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowFileManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('FileManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowFileManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('FileManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[5].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/FileCertificate.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowFileCertificateText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('FileCertificate')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowFileCertificateText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('FileCertificate')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[6].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/FileUnlock.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowFileUnlockText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('FileUnlock')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowFileUnlockText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('FileUnlock')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>
                        </NavigationViewItem.MenuItems>
                    </NavigationViewItem>

                    <!--  资源工具  -->
                    <NavigationViewItem Tag="{x:Bind PageList[7].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/Resource.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowResourceText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Resource')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowResourceText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Resource')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>

                        <NavigationViewItem.MenuItems>
                            <NavigationViewItem Tag="{x:Bind PageList[8].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/DataVerifyEncrypt.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowDataVerifyEncryptText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('DataVerifyEncrypt')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowDataVerifyEncryptText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('DataVerifyEncrypt')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[9].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/DownloadManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowDownloadManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('DownloadManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowDownloadManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('DownloadManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[10].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/IconExtract.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowIconExtractText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('IconExtract')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowIconExtractText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('IconExtract')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[11].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/PriExtract.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowPriExtractText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('PriExtract')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowPriExtractText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('PriExtract')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>
                        </NavigationViewItem.MenuItems>
                    </NavigationViewItem>

                    <!--  个性化工具  -->
                    <NavigationViewItem Tag="{x:Bind PageList[12].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/Personalize.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowPersonalizeText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Personalize')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowPersonalizeText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Personalize')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>

                        <NavigationViewItem.MenuItems>
                            <NavigationViewItem Tag="{x:Bind PageList[13].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/ThemeSwitch.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowThemeSwitchText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ThemeSwitch')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowThemeSwitchText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ThemeSwitch')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[14].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/ShellMenu.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowShellMenuText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ShellMenu')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowShellMenuText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ShellMenu')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[15].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/ContextMenuManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowContextMenuManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ContextMenuManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowContextMenuManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ContextMenuManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[16].Key}" Visibility="Collapsed">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/ExperimentalFeatureManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowExperimentalFeatureManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ExperimentalFeatureManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowExperimentalFeatureManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ExperimentalFeatureManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>
                        </NavigationViewItem.MenuItems>
                    </NavigationViewItem>

                    <!--  系统工具  -->
                    <NavigationViewItem Tag="{x:Bind PageList[17].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/System.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowSystemText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('System')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowSystemText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('System')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>

                        <NavigationViewItem.MenuItems>
                            <NavigationViewItem Tag="{x:Bind PageList[18].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/LoopbackManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowLoopbackManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('LoopbackManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowLoopbackManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('LoopbackManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[19].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/ScheduledTaskManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowScheduledTaskManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ScheduledTaskManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowScheduledTaskManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('ScheduledTaskManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[20].Key}" Visibility="Collapsed">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/Hosts.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowHostsText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Hosts')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowHostsText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Hosts')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[21].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/DriverManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowDriverManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('DriverManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowDriverManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('DriverManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[22].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/UpdateManager.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowUpdateManagerText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('UpdateManager')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowUpdateManagerText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('UpdateManager')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[23].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/WinFR.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowWinFRText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('WinFR')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowWinFRText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('WinFR')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>

                            <NavigationViewItem Tag="{x:Bind PageList[24].Key}">

                                <NavigationViewItem.Icon>
                                    <ImageIcon Source="ms-appx:///Assets/ControlIcon/WinSAT.png" />
                                </NavigationViewItem.Icon>

                                <TextBlock
                                    x:Name="WindowWinSATText"
                                    Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('WinSAT')}"
                                    TextTrimming="CharacterEllipsis"
                                    TextWrapping="NoWrap" />

                                <ToolTipService.ToolTip>
                                    <ToolTip
                                        MaxWidth="9999"
                                        Padding="0"
                                        Background="Transparent"
                                        Visibility="{x:Bind WindowWinSATText.IsTextTrimmed, Mode=OneWay}">

                                        <Grid>
                                            <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                            <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('WinSAT')}" />
                                        </Grid>
                                    </ToolTip>
                                </ToolTipService.ToolTip>
                            </NavigationViewItem>
                        </NavigationViewItem.MenuItems>
                    </NavigationViewItem>
                </NavigationView.MenuItems>

                <NavigationView.FooterMenuItems>
                    <NavigationViewItem Tag="{x:Bind PageList[25].Key}">

                        <NavigationViewItem.Icon>
                            <ImageIcon Source="ms-appx:///Assets/ControlIcon/Settings.png" />
                        </NavigationViewItem.Icon>

                        <TextBlock
                            x:Name="WindowSettingsText"
                            Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Settings')}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />

                        <ToolTipService.ToolTip>
                            <ToolTip
                                MaxWidth="9999"
                                Padding="0"
                                Background="Transparent"
                                Visibility="{x:Bind WindowSettingsText.IsTextTrimmed, Mode=OneWay}">

                                <Grid>
                                    <SystemBackdropElement CornerRadius="{StaticResource ControlCornerRadius}" SystemBackdrop="{StaticResource AcrylicBackgroundFillColorDefaultBackdrop}" />
                                    <TextBlock Padding="{StaticResource ToolTipBorderPadding}" Text="{x:Bind rootservice:ResourceService.WindowResource.GetString('Settings')}" />
                                </Grid>
                            </ToolTip>
                        </ToolTipService.ToolTip>
                    </NavigationViewItem>
                </NavigationView.FooterMenuItems>

                <Frame
                    Margin="0,-10,0,0"
                    Navigated="{x:Bind OnNavigated}"
                    NavigationFailed="{x:Bind OnNavigationFailed}">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition />
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </NavigationView>

            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="BackgroundDefault">
                        <VisualState.Setters>
                            <Setter Target="MainPage.Background" Value="{ThemeResource DefaultBackgroundColorBrush}" />
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="BackgroundTransparent" />
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>
        </Grid>
    </Page>
</Window>
